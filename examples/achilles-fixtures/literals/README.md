# Optional literals scan fixture

Hardcoded values for the opt-in `literals` engine (`goose appsec scan --literals`). These are **not** security findings.

```bash
goose appsec scan --path examples/achilles-fixtures/literals --literals
```
