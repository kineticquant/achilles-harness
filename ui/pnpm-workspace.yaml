packages:
  - 'sdk'
  - 'desktop'
  - 'goose-binary/*'

registry: https://registry.npmjs.org/
preferOffline: true
nodeLinker: hoisted
supportedArchitectures:
  os: [ current, linux, darwin, win32 ]
  cpu: [ current, x64, arm64 ]
overrides:
  react: ^19.2.8
  react-dom: ^19.2.8
allowBuilds:
  '@modelcontextprotocol/ext-apps': true
  electron: true
  electron-winstaller: true
  esbuild: true
  # Darwin-only native addons for maker-dmg / appdmg. Allowed so pnpm 10+
  # does not abort install with ERR_PNPM_IGNORED_BUILDS; scripts no-op on Windows.
  fs-xattr: true
  macos-alias: true
# Electron Forge resolves @electron/node-gyp from git.
blockExoticSubdeps: false
